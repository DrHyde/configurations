# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
name = David Cantrell
email = david@cantrell.org.uk
[push]
	default = simple
[pull]
	rebase = false
[core]
	editor = vim -u ~/.vimrc-basic
	pager = "if [ ! -z \"$(which diff-so-fancy|grep -v \"no diff-so-fancy in\")\" ]; then diff-so-fancy | less --tabs=4 -RFX; else less; fi"
[diff-so-fancy]
	markEmptyLines = false
